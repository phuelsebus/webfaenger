[project]
name = "webfaenger"
version = "0.1.0"
description = "Leichtgewichtiger Desktop-Downloader für Bilder von Webseiten"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
